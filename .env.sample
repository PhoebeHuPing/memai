GOOGLE_GENERATIVE_AI_API_KEY=
LLAMA_CLOUD_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Optional: API authentication. When set, all /api/ requests require
# Authorization: Bearer <key> header. Leave empty for open access (dev mode).
API_SECRET_KEY=

# Optional: Comma-separated allowed CORS origins. Defaults to "*" (all).
# ALLOWED_ORIGINS=http://localhost:5173,https://your-domain.com
