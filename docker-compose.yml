services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      # Persist SQLite database and ChromaDB between restarts
      - app-data:/app/server/data
    environment:
      - PYTHONUNBUFFERED=1

volumes:
  app-data:
